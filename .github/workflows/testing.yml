name: Project-1 Workflow

on:
  push:
    branches:
      - prod

jobs:
  use-reusable-pipeline:
    uses: ankitagrawal10/test-1/.github/workflows/reuseable.yml@master
    with:
      python_version: '3.12'

  run-code:
    runs-on: self-hosted
    steps:
      - name: Run python code
        run: | 
         echo -e "5" | python app.py
         echo -e "10" | python app.py

     
